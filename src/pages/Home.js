import React, { Component } from "react";

export class Home extends Component {
  render() {
    return (
      <div className="container">
        <h4 className="section-header">Home</h4>
        <p>/:food_id endpoint is setup somewhat already</p>
        <p>
          Need to add recipes or stuff here. Query recipes with links to them.
        </p>
      </div>
    );
  }
}

export default Home;
